image: erlang:20

before_script:
  - apt-get update -y
  - apt-get -y install g++-4.9
  - export CXX=g++-4.9

test:
  script:
    - ./rebar3 eunit
  cache:
    paths:
    - c_src/rocksdb
    - c_src/system
